# 智能饮食健康管理平台 — 开发任务清单

> 基于代码审计结果，按优先级排序
> 更新日期：2026-05-29

---

## 优先级定义

| 级别 | 含义 | 答辩影响 |
|------|------|----------|
| **P0** | 影响功能正确性，老师演示时会发现 | 直接扣分 |
| **P1** | 提升工程质量，展示开发成熟度 | 加分项 |
| **P2** | 锦上添花，有时间再做 | 不影响评分 |

---

## P0 任务（预计 8-10 小时）

### 任务 1：修复 5 个 Critical Bug

| 属性 | 内容 |
|------|------|
| **目标** | 消除所有运行时错误 |
| **预计时间** | 2 小时 |
| **验收标准** | ① 饮食建议页面 API 失败时显示错误提示而非白屏 ② 饮水/运动页面 16:00 后显示当天日期 ③ Docker 部署后所有 11 张表存在 ④ 管理员可正常删除用户 |
| **具体工作** | 1. `DietAdviceView.vue` 第 248 行附近添加 `import { ElMessage } from 'element-plus'` 2. `WaterRecordView.vue` 第 118 行改为 `ref(getToday())` 3. `ExerciseRecordView.vue` 第 120 行改为 `ref(getToday())` 4. `docker-compose.yml` 添加 `expand_tables.sql` 挂载 5. `sql/init.sql` 第 49 行 FK 添加 `ON DELETE CASCADE` |
| **涉及文件** | `DietAdviceView.vue`、`WaterRecordView.vue`、`ExerciseRecordView.vue`、`docker-compose.yml`、`sql/init.sql` |

### 任务 2：修复 Docker 部署

| 属性 | 内容 |
|------|------|
| **目标** | `docker-compose up -d` 一键启动全部服务且健康检查通过 |
| **预计时间** | 1.5 小时 |
| **验收标准** | ① `docker-compose ps` 三个服务均显示 healthy ② 前端可正常访问所有页面 ③ 数据库包含所有 11 张表 |
| **具体工作** | 1. 后端添加 `spring-boot-starter-actuator` 依赖 2. 健康检查改为 `/actuator/health` 3. 挂载全部 3 个 SQL 文件 4. 添加 `restart: unless-stopped` 5. 设置 `JWT_SECRET` 默认值 |
| **涉及文件** | `pom.xml`、`docker-compose.yml`、`application.yml` |

### 任务 3：补充 FamilyMemberService 单元测试

| 属性 | 内容 |
|------|------|
| **目标** | 核心业务逻辑有独立测试保护 |
| **预计时间** | 1.5 小时 |
| **验收标准** | ① 新建 `FamilyMemberServiceTest.java` ② 覆盖：列表查询、详情查询（含所有权校验）、创建（含 10 人上限校验）、更新、删除 ③ 全部通过 |
| **具体工作** | 参考 `UserServiceTest.java` 的 Mock 模式，Mock `FamilyMemberMapper` 和 `UserMapper`，测试 6+ 个场景 |
| **涉及文件** | 新建 `src/test/java/com/diet/service/FamilyMemberServiceTest.java` |

### 任务 4：补充控制器集成测试

| 属性 | 内容 |
|------|------|
| **目标** | 控制器测试覆盖率从 30% 提升到 60%+ |
| **预计时间** | 3-4 小时 |
| **验收标准** | ① 新增 5 个控制器测试文件 ② 每个文件 3+ 个测试方法（CRUD + 权限校验） ③ 新增 15+ 测试用例全部通过 |
| **具体工作** | 为 `WaterRecordController`、`ExerciseRecordController`、`WeightRecordController`、`HealthGoalController`、`FavoriteController` 编写 MockMvc 测试。参考 `DietRecordControllerTest` 的认证和跨用户权限测试模式。 |
| **涉及文件** | 新建 5 个测试文件于 `src/test/java/com/diet/controller/` |

### 任务 5：数据库加固

| 属性 | 内容 |
|------|------|
| **目标** | 在数据库层面强制数据完整性 |
| **预计时间** | 1 小时 |
| **验收标准** | ① CHECK 约束拒绝非法 gender/severity/activity_level/goal 值 ② 新索引在 EXPLAIN 中显示被使用 |
| **具体工作** | 1. 为 `user.gender` 添加 `CHECK (gender IN ('male', 'female'))` 2. 为 `body_symptom.severity` 添加 `CHECK (severity BETWEEN 1 AND 5)` 3. 为 `user.activity_level` 添加 CHECK 约束 4. 创建索引：`idx_food_category`、`idx_user_role`、`idx_detail_food`、`idx_goal_status` |
| **涉及文件** | `sql/init.sql`、`sql/expand_tables.sql` |

---

## P1 任务（预计 6-8 小时）

### 任务 6：前端 Bug 修复和代码清理

| 属性 | 内容 |
|------|------|
| **目标** | 修复前端 Bug，统一代码风格 |
| **预计时间** | 2 小时 |
| **验收标准** | ① pinyin.js 无重复 key ② 删除 `api/recent.js`，所有引用改为从 `api/diet.js` 导入 ③ 错误处理统一为 Pattern A（`ElMessage.error`） ④ 无功能回归 |
| **具体工作** | 1. 修复 pinyin.js 4 处重复 key（选择正确的拼音值） 2. 删除 `src/api/recent.js`，更新所有 import 3. 将 `console.warn` 模式改为 `ElMessage.error`（涉及 `HomeView`、`NutritionStatsView`、`DietAdviceView`） 4. 为 `FoodLibraryView.loadFavoriteIds` 的空 catch 添加错误提示 |
| **涉及文件** | `pinyin.js`、`recent.js`、`HomeView.vue`、`NutritionStatsView.vue`、`DietAdviceView.vue`、`FoodLibraryView.vue` |

### 任务 7：拆分 DietAdviceView.vue

| 属性 | 内容 |
|------|------|
| **目标** | 将 946 行单文件拆分为 4 个子组件，每个 < 300 行 |
| **预计时间** | 2 小时 |
| **验收标准** | ① `DietAdviceView.vue` 变为容器组件（< 150 行） ② 新建 3 个子组件：`DailyAdviceTab.vue`、`DietAnalysisTab.vue`、`HealthAdviceTab.vue` ③ 每个子组件 < 300 行 ④ 功能与拆分前完全一致 |
| **具体工作** | 1. 提取每日建议面板为 `DailyAdviceTab.vue` 2. 提取饮食分析面板（含 ECharts）为 `DietAnalysisTab.vue` 3. 提取健康建议面板为 `HealthAdviceTab.vue` 4. 父组件通过 `v-if` 控制 tab 切换，子组件自行管理 loading 和数据获取 |
| **涉及文件** | `src/views/DietAdviceView.vue`、新建 3 个 `src/components/` 文件 |

### 任务 8：补充 E2E 测试

| 属性 | 内容 |
|------|------|
| **目标** | 覆盖水/运动/体重记录的核心用户流程 |
| **预计时间** | 2-3 小时 |
| **验收标准** | ① 新增 3 个 spec 文件 ② 新增 15+ 测试用例 ③ 全部通过 ④ 修复现有测试中的 `waitForTimeout` 和条件守卫问题 |
| **具体工作** | 1. 新建 `water-record.spec.js`：添加记录、删除记录、查看统计 2. 新建 `exercise-record.spec.js`：添加记录、编辑记录、查看趋势 3. 新建 `weight-record.spec.js`：添加记录、查看趋势图 4. 修复 `food-library.spec.js` 和 `diet-record.spec.js` 中的 `waitForTimeout(500)` 和 `if (await ...)` 模式 |
| **涉及文件** | 新建 3 个 `e2e/*.spec.js`、修改 `food-library.spec.js`、`diet-record.spec.js` |

### 任务 9：更新论文测试数据

| 属性 | 内容 |
|------|------|
| **目标** | 论文中声称的测试数量与实际一致 |
| **预计时间** | 0.5 小时 |
| **验收标准** | ① 运行 `mvn test` 和 `npx playwright test` 获取实际数量 ② 更新论文中的测试表格 ③ `.spec.ts` 改为 `.spec.js` ④ 测试方法名与实际文件匹配 |
| **具体工作** | 运行测试 → 统计数量 → 更新 `docs/毕业论文.md` 第 6 章测试部分 |
| **涉及文件** | `docs/毕业论文.md` |

---

## P2 任务（预计 4-6 小时，有时间再做）

| ID | 任务 | 时间 | 验收标准 |
|----|------|------|----------|
| 10 | Element Plus 按需导入 | 1.5h | 安装 `unplugin-vue-components` + `unplugin-auto-import`，配置 Vite 插件，打包体积减少 40%+ |
| 11 | 添加 ESLint + Prettier | 1h | `npm run lint` 无报错，代码风格统一 |
| 12 | 收藏状态提升到 Pinia | 1h | 新建 `useFavoriteStore`，`FoodLibraryView` 和 `DietRecordView` 共用同一状态 |
| 13 | CSS 变量统一 | 1h | 所有 View 使用 `variables.css` 中的变量，无硬编码颜色值 |

---

## 执行计划

```
Day 1 (4h): 任务1 + 任务2 + 任务5
           └── Bug 修复 + Docker 修复 + 数据库加固

Day 2 (4h): 任务3 + 任务4
           └── 补充后端测试

Day 3 (4h): 任务6 + 任务7
           └── 前端代码清理 + 组件拆分

Day 4 (3h): 任务8 + 任务9
           └── E2E 测试 + 论文数据更新

Day 5+ (可选): 任务10-13
           └── 工程化优化
```

---

## 测试统计预期

| 指标 | 当前 | P0 完成后 | P1 完成后 |
|------|------|-----------|-----------|
| 后端单元测试 | 105 | 111+ | 111+ |
| 后端控制器测试 | 40 | 55+ | 55+ |
| E2E 测试 | 33 | 33 | 48+ |
| 控制器测试覆盖 | 4/13 (31%) | 9/13 (69%) | 9/13 (69%) |
